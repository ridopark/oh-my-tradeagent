{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://oh-my-tradeagent/schemas/adoption-workflow-input.json",
  "title": "AdoptionWorkflowInput",
  "description": "Issue #285: input to the short-lived AdoptionWorkflow started by api-gateway POST /positions/adopt. Identifies the confirmed orphan (tenant, strategy, occ) to adopt and the operator who triggered it (provenance / audit). The workflow resolves broker_target from StrategyConfig to route its broker-truth ReconciliationExecActivity calls to the correct exec task queue (broker-<broker_target>); it is not an input field.",
  "type": "object",
  "javaType": "com.ohmytradeagent.contract.AdoptionWorkflowInput",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "tenant_id",
    "strategy_id",
    "occ",
    "operator_id"
  ],
  "properties": {
    "schema_version": { "type": "integer", "minimum": 1 },
    "tenant_id": { "type": "string", "minLength": 1 },
    "strategy_id": { "type": "string", "minLength": 1 },
    "occ": {
      "type": "string",
      "minLength": 1,
      "description": "The orphaned OCC option symbol to adopt. Operator may supply the broker/audit compact form or the padded OccSymbol.of form; the workflow resolves the canonical padded form from the journal anchor."
    },
    "operator_id": {
      "type": "string",
      "minLength": 1,
      "description": "Operator who triggered the adoption (from X-Operator-Id). Recorded in the PositionAdopted audit provenance."
    }
  }
}
