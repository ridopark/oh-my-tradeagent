{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://oh-my-tradeagent/schemas/partial-exit-request.json",
  "title": "PartialExitRequest",
  "description": "Signal payload to PositionWorkflow.partial_exit. Dispatched by CopytradeSignalWorkflow STC branch after KeywordPartialMatcher resolves the fraction. signal_id is the dedupe key inside the position.",
  "type": "object",
  "javaType": "com.ohmytradeagent.contract.PartialExitRequest",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "tenant_id",
    "strategy_id",
    "signal_id",
    "position_workflow_id",
    "fraction",
    "reason",
    "author",
    "raw_line",
    "occurred_at"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "DTO contract version. See CopytradeSignalPayload.schema_version."
    },
    "tenant_id": {
      "type": "string",
      "minLength": 1
    },
    "strategy_id": {
      "type": "string",
      "minLength": 1
    },
    "signal_id": {
      "type": "string",
      "minLength": 1,
      "description": "Dedupe key inside the receiving PositionWorkflow. Shape: '<message_id>:<line_index>'."
    },
    "position_workflow_id": {
      "type": "string",
      "minLength": 1,
      "description": "Target PositionWorkflow workflow_id (resolved via Redis cache + Visibility fallback)."
    },
    "fraction": {
      "type": "number",
      "exclusiveMinimum": 0,
      "maximum": 1,
      "description": "Fraction of remaining qty to close. Resolved from KeywordPartialMatcher.match(tail, cfg.partial_fractions, cfg.default_stc_fraction)."
    },
    "ref_premium": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "Author-posted exit premium (for audit; not source-of-truth for fill price). Also SEEDS the exit LIMIT price in PositionWorkflowImpl.exitIntent. OPTIONAL: null/absent places the exit marketable (the de-risk-cue dispatcher already passes a null target_entry_premium), and it is unused entirely when market=true."
    },
    "market": {
      "type": "boolean",
      "description": "Place the partial SELL as a MARKET order (limit_price=null) instead of the ref_premium-seeded bounded limit + reprice ladder. Set by the operator-initiated PositionWorkflow.partial_close Update (the /live 'Trim' button), whose contract is exit-NOW like force_close. Null/absent/false keeps the bounded-limit STC behavior — every pre-existing dispatcher (STC, de-risk cue) omits it, so their placement is unchanged."
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "description": "Why the exit fired (e.g., 'stc_signal')."
    },
    "author": {
      "type": "string",
      "minLength": 1,
      "description": "Discord author who posted the STC line."
    },
    "raw_line": {
      "type": "string",
      "minLength": 1,
      "description": "The STC line as parsed, for audit."
    },
    "occurred_at": {
      "type": "string",
      "format": "date-time",
      "description": "RFC3339 UTC timestamp when the signal was emitted (sidecar source-of-truth)."
    }
  }
}
