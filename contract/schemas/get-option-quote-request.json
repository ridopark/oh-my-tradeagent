{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://oh-my-tradeagent/schemas/get-option-quote-request.json",
  "title": "GetOptionQuoteRequest",
  "description": "Plan-2A R-AA-2: request from PositionWorkflow to market-data-svc for a one-shot NBBO snapshot of an option contract. The snapshot anchors bounded scheduled-flatten sells (eod/expiry/chandelier) on a real live bid instead of a market order. Pure read; mirrors SubscribePremiumRequest's tenant/strategy scoping.",
  "type": "object",
  "javaType": "com.ohmytradeagent.contract.GetOptionQuoteRequest",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "tenant_id",
    "strategy_id",
    "contract_symbol"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "minimum": 1,
      "description": "DTO contract version."
    },
    "tenant_id": {
      "type": "string",
      "minLength": 1
    },
    "strategy_id": {
      "type": "string",
      "minLength": 1
    },
    "contract_symbol": {
      "type": "string",
      "minLength": 1,
      "description": "OCC option symbol to snapshot."
    }
  }
}
