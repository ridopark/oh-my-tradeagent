{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://oh-my-tradeagent/schemas/kill-switch-workflow-input.json",
  "title": "KillSwitchWorkflowInput",
  "description": "Input for KillSwitchWorkflow (Phase 5). One workflow per (tenant_id, strategy_id), bootstrapped on orchestrator startup. Long-running; the workflow now invokes continueAsNew once history-length crosses a watermark (issue #124) and carries the optional state fields below across the boundary. Fresh bootstrap inputs omit the carry-forward fields; replayed continueAsNew inputs populate them.",
  "type": "object",
  "javaType": "com.ohmytradeagent.contract.KillSwitchWorkflowInput",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "tenant_id",
    "strategy_id"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "minimum": 1,
      "maximum": 2,
      "description": "DTO contract version. v1 = bootstrap-only input; v2 adds optional carry-forward fields produced by continueAsNew. Workers reject newer-than-build inputs."
    },
    "tenant_id": {
      "type": "string",
      "minLength": 1
    },
    "strategy_id": {
      "type": "string",
      "minLength": 1
    },
    "tripped": {
      "type": "boolean",
      "description": "Carry-forward state from a prior run. Absent on fresh bootstrap. Default false."
    },
    "reason": {
      "type": "string",
      "description": "Carry-forward trip reason from a prior run. Absent on fresh bootstrap."
    },
    "actor": {
      "type": "string",
      "description": "Carry-forward trip actor from a prior run. Absent on fresh bootstrap."
    },
    "tripped_at": {
      "type": "string",
      "format": "date-time",
      "description": "Carry-forward trip timestamp from a prior run. Absent on fresh bootstrap."
    },
    "cooling_down_until": {
      "type": "string",
      "format": "date-time",
      "description": "Carry-forward reset cooldown deadline from a prior run. Absent on fresh bootstrap."
    },
    "trading_day": {
      "type": "string",
      "format": "date",
      "description": "Carry-forward trading day from a prior run. Absent on fresh bootstrap; next heartbeat refreshes via calendar.todayEt()."
    },
    "last_still_tripped_page_day": {
      "type": "string",
      "format": "date",
      "description": "#669 carry: the trading day the once-per-day still-tripped page last fired (or was quiet-stamped on the trip day). Carried across continue-as-new so the roll day neither loses its page nor duplicates it; absent on a pre-#669 carry, where the first tripped tick quiet-stamps (one page skipped, once, ever)."
    }
  }
}
