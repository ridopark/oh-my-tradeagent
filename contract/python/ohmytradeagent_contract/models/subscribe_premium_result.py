# generated by datamodel-codegen:
#   filename:  subscribe-premium-result.json

from __future__ import annotations

from enum import StrEnum
from typing import Annotated

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field


class Status(StrEnum):
    """
    Outcome. FAILED returns instead of throwing so the workflow can audit and continue.
    """

    subscribed = "SUBSCRIBED"
    failed = "FAILED"


class SubscribePremiumResult(BaseModel):
    """
    Phase 4: result of SubscribePremiumActivity.subscribePremium. status=FAILED is returned (not thrown) so the workflow can audit and proceed without trail.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    schema_version: Annotated[int, Field(ge=1)]
    """
    DTO contract version.
    """
    subscription_id: str
    """
    Opaque ID for the active subscription. Empty string when status=FAILED.
    """
    subscribed_at: AwareDatetime
    """
    RFC3339 UTC timestamp when the subscription was created (or attempted).
    """
    status: Status
    """
    Outcome. FAILED returns instead of throwing so the workflow can audit and continue.
    """
    error: str | None = None
    """
    Error message when status=FAILED.
    """
