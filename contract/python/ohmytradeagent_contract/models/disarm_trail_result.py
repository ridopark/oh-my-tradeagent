# generated by datamodel-codegen:
#   filename:  disarm-trail-result.json

from __future__ import annotations

from enum import StrEnum

from pydantic import BaseModel, ConfigDict


class Status(StrEnum):
    """
    DISARMED = protection removed (page fired). NOT_ARMED = nothing was armed; no state changed.
    """

    disarmed = "DISARMED"
    not_armed = "NOT_ARMED"


class DisarmTrailResult(BaseModel):
    """
    Result of PositionWorkflow.disarm_trail (#825). DISARMED carries the stop that was just removed (prior peak/giveback and the threshold it implied) so the operator can restate it on re-arm; NOT_ARMED is the honest no-op.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    schema_version: int
    """
    Payload schema version; 1.
    """
    status: Status
    """
    DISARMED = protection removed (page fired). NOT_ARMED = nothing was armed; no state changed.
    """
    prior_peak_premium: float | None = None
    """
    The peak the removed trail was anchored on (present on DISARMED).
    """
    prior_giveback_pct: float | None = None
    """
    The giveback fraction of the removed trail (present on DISARMED).
    """
    detail: str | None = None
    """
    Human-readable summary.
    """
