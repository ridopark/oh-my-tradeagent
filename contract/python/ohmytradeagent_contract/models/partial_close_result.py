# generated by datamodel-codegen:
#   filename:  partial-close-result.json

from __future__ import annotations

from enum import StrEnum
from typing import Annotated

from pydantic import BaseModel, ConfigDict, Field


class Status(StrEnum):
    """
    ACCEPTED: the trim was enqueued (the SELL is placed by the main loop). NOOP_ALREADY_CLOSED: the position was already drained (remainingQty == 0 on a confirmed position) — nothing was enqueued.
    """

    accepted = "ACCEPTED"
    noop_already_closed = "NOOP_ALREADY_CLOSED"


class PartialCloseResult(BaseModel):
    """
    Synchronous return value of PositionWorkflow.partial_close (WaitPolicy=Accepted). Mirrors ForceCloseResult: exit_signal_id is the dedupe key inside the workflow's exit machinery, returned in the 202 response so the operator can correlate the eventual fill.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    schema_version: Annotated[int, Field(ge=1)]
    status: Status
    """
    ACCEPTED: the trim was enqueued (the SELL is placed by the main loop). NOOP_ALREADY_CLOSED: the position was already drained (remainingQty == 0 on a confirmed position) — nothing was enqueued.
    """
    exit_signal_id: Annotated[str, Field(min_length=1)]
    """
    Dedupe key of the synthesized partial exit inside the position ('trim:<operator_id>:<workflow_millis>').
    """
