# generated by datamodel-codegen:
#   filename:  subscribe-equity-result.json

from __future__ import annotations

from enum import StrEnum
from typing import Annotated

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field


class Status(StrEnum):
    """
    Outcome. FAILED (source error) and GATED (live use not enabled / fail-closed) return instead of throwing so the workflow can audit and continue.
    """

    subscribed = "SUBSCRIBED"
    failed = "FAILED"
    gated = "GATED"


class SubscribeEquityResult(BaseModel):
    """
    Phase 2 (watchlist-trigger): result of SubscribeEquityActivity.subscribeEquity. status=FAILED is returned (not thrown) so the workflow can audit and proceed without a feed. status=GATED is returned when live use is not enabled (stock WS not configured) — the worker fails closed and does not connect.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    schema_version: Annotated[int, Field(ge=1)]
    """
    DTO contract version.
    """
    subscription_id: str
    """
    Opaque ID for the active subscription. Empty string when status!=SUBSCRIBED.
    """
    subscribed_at: AwareDatetime
    """
    RFC3339 UTC timestamp when the subscription was created (or attempted).
    """
    status: Status
    """
    Outcome. FAILED (source error) and GATED (live use not enabled / fail-closed) return instead of throwing so the workflow can audit and continue.
    """
    error: str | None = None
    """
    Error message when status=FAILED or GATED.
    """
