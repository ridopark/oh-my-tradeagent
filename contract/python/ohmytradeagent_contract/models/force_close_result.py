# generated by datamodel-codegen:
#   filename:  force-close-result.json

from __future__ import annotations

from enum import StrEnum
from typing import Annotated

from pydantic import BaseModel, ConfigDict, Field


class Status(StrEnum):
    """
    ACCEPTED: cancel-then-marketable-sell sequence queued. NOOP_ALREADY_CLOSED: position already at remainingQty=0 (e.g. another exit just filled).
    """

    accepted = "ACCEPTED"
    noop_already_closed = "NOOP_ALREADY_CLOSED"


class ForceCloseResult(BaseModel):
    """
    Synchronous return value of PositionWorkflow.force_close Update (WaitPolicy=Accepted). exit_signal_id is the dedupe key inside the workflow's exit machinery; api-gateway returns it in the 202 response so the operator can correlate the eventual fill.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    schema_version: Annotated[int, Field(ge=1)]
    status: Status
    """
    ACCEPTED: cancel-then-marketable-sell sequence queued. NOOP_ALREADY_CLOSED: position already at remainingQty=0 (e.g. another exit just filled).
    """
    exit_signal_id: Annotated[str, Field(min_length=1)]
    """
    Deterministic exit dedupe key. Shape: 'force:<operator_id>:<offset>'.
    """
